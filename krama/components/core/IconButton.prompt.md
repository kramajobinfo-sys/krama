Compact icon-only button for toolbars, table rows, and card corners (bookmark, more, close). Always pass `aria-label`.

```jsx
<IconButton aria-label="Save job"><i data-lucide="bookmark" /></IconButton>
<IconButton variant="secondary" aria-label="More"><i data-lucide="ellipsis" /></IconButton>
```
