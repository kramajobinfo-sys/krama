Lifecycle status indicator for jobs, companies, and applications. Shows a colored dot + label; pass `status` and it picks the right color pair and default label.

```jsx
<StatusBadge status="pending" />
<StatusBadge status="published" />
<StatusBadge status="rejected">Rejected · duplicate</StatusBadge>
```
