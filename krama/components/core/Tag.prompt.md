Outlined chip for skills, job categories, and filter tokens. Use `active` for selected filters and `removable` for applied-filter tokens.

```jsx
<Tag icon={<i data-lucide="map-pin" />}>Phnom Penh</Tag>
<Tag active>Full-time</Tag>
<Tag removable onRemove={clear}>Remote</Tag>
```
