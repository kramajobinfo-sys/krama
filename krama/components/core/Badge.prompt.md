Small pill for counts, category labels, and inline metadata. Use `tone` for meaning; `solid` for high-emphasis counts (e.g. notification bubble).

```jsx
<Badge tone="accent">Featured</Badge>
<Badge tone="brand">Remote</Badge>
<Badge tone="danger" solid>3</Badge>
```
