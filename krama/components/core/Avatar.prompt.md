Person or company avatar. Pass `name` for initials + auto tint; `square` for company logos.

```jsx
<Avatar name="Sok Dara" size={44} />
<Avatar src="/logo.png" name="ABA Bank" square size={48} />
```
