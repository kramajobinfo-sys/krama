Base surface container. Wrap any grouped content; set `interactive` for clickable cards and `featured` for a saffron top-accent (featured jobs/companies).

```jsx
<Card interactive onClick={open}>…</Card>
<Card featured padding={24}>…</Card>
```
