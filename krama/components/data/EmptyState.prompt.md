Encouraging placeholder for empty lists (no applications, no saved jobs). Keep copy supportive and action-led.

```jsx
<EmptyState icon={<i data-lucide="file-text" />} title="No applications yet"
  description="When you apply, you'll track every step right here."
  action={<Button>Browse jobs</Button>} />
```
