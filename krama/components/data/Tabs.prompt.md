Underline tab bar for switching views (e.g. application states, dashboard sections). Optional `count` badge per tab.

```jsx
<Tabs value={tab} onChange={setTab}
  tabs={[{value:'all',label:'All',count:48},{value:'pending',label:'Pending',count:6}]} />
```
