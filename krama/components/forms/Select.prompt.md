Dropdown select matching the Input look, with a custom chevron. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Category" placeholder="All categories"
  options={["IT","Finance","Marketing","Engineering"]} />
```
