Single-line text field with label, hint, error and optional leading icon. Focus shows the teal ring; `error` turns it red.

```jsx
<Input label="Email" type="email" placeholder="you@example.com" iconLeft={<i data-lucide="mail" />} />
<Input label="Job title" error="This field is required" />
```
